on:
  push:
    branches:
      - main

jobs:
  print_context:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v2
      - env:
          EVENT_CONTEXT: ${{ toJSON(github.event) }}
        run: |
          echo $EVENT_CONTEXT
      - name: Rebase 2
        run: |
          echo $EVENT_CONTEXT
      - name: Rebase
        run: |
          python rebaser.py
